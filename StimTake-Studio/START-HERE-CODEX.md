# START HERE — StimTake Studio Codex Handoff

This is a source/recovery checkpoint, not a release.

Read in this order:

1. `README.md`
2. `_SAFEPOINT_EVIDENCE/CODEX-HANDOFF.md`
3. `_SAFEPOINT_EVIDENCE/VALIDATION.txt`
4. `StimTake-Chrome-Bridge-v2.0/README.md`

For the current Windows build path, preserve the repository first, then use the
existing project build workflow only after inspecting `git status`.

Known unresolved defect: OBS Top Tippers/Fans can still show stale TestViewer
state after loading the real saved list.
